import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';
import { Component, } from '@angular/core';
import '../../content/style/style.scss';

@Component({
    selector: 'app',
    template: require('./app.page.html')
})
export class AppPage {
    
}